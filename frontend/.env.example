# Base URL for the backend API. Leave empty to use the Vite dev proxy in
# development. Set to the deployed API origin for a production build.
VITE_API_BASE=

# Target the Vite dev proxy forwards /api requests to during local development.
VITE_PROXY_TARGET=http://localhost:8080
